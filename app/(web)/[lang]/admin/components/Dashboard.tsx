// This component will be the main view for authenticated users who have completed onboarding.
// It will display key statistics, API key lists, and provide access to configuration and logs.
// For now, it's a simple placeholder.

"use client";

export function Dashboard() {
  return (
    <div>
      <h2 className="text-2xl font-semibold">Dashboard</h2>
      <p>Dashboard placeholder: Key stats and management tools will be here.</p>
    </div>
  );
}
